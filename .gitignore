# Python
__pycache__/
*.py[cod]
*$py.class
*.egg-info/
.pytest_cache/
.coverage
.venv/
venv/
build/
dist/

# Editor / OS
.DS_Store
*.swp
.vscode/
.idea/
.claude/

# LibreOffice / Excel lock files
.~lock.*#
~$*

# Local SQLite databases (incl. WAL/journal artifacts)
*.db
*.db-journal
*.db-wal
*.db-shm
*.sqlite
*.sqlite3

# Local analysis outputs (regenerable)
output/
FSP Exports/

# Real alumni survey responses (PII — name, email, training timeline).
# Synthetic equivalent (synthetic_alumni_survey.csv) stays checked in for tests.
alumni_survey.xlsx

# Local env (auth secret, admin creds). Template lives in .env.example.
.env
.env.local
.env.*.local

# Ruflo MCP local state
agentdb.rvf
agentdb.rvf.lock

# Local Python virtualenvs (additional patterns)
.venv_ci/

# Designer / non-code source bundles
design_handoff_provectus_analytics/

# Root-level node_modules (frontend/node_modules already covered above)
/node_modules/

# Frontend
frontend/node_modules/
frontend/dist/
frontend/.vite/
